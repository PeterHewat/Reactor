# Copy to .env.local for local marketing dev (optional)
# Same repository URL as VITE_REPO_URL in apps/web/.env.local
PUBLIC_REPO_URL=https://github.com/YOUR_ORG/YOUR_REPO
# Optional: default branch for README links (default: main)
# PUBLIC_REPO_DEFAULT_BRANCH=main
