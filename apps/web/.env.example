# ============================================================================
# Web App Environment Variables
# ============================================================================
# Copy to .env.local and fill in your values (see comments below).
# ============================================================================

# Convex — URL from `bun run dev:convex` or the Convex dashboard
VITE_CONVEX_URL=https://your-project.convex.cloud

# Clerk — Configure → API keys → React → publishable key
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your-key-here

# Clerk — Configure → API keys → secret key (Playwright E2E only; not sent to the browser)
CLERK_SECRET_KEY=sk_test_your-secret-key

# Clerk — dev user with Email + Password (see docs/development.md#e2e-tests-playwright)
E2E_CLERK_USER_EMAIL=you+clerk_test@example.com
