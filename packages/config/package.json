{
  "name": "@repo/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vite/Vitest aliases, product name, repo URLs, env placeholders",
  "exports": {
    "./aliases": "./aliases.ts",
    "./product": "./product.ts",
    "./env-placeholders": "./env-placeholders.ts",
    "./repo-url": "./repo-url.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
